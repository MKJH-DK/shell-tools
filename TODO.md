# TODO

- [ ] Add more utility scripts to `scripts/` (e.g., docker helpers, network tools).
- [ ] Improve error handling in `install.sh`.
- [ ] Add automated tests for the `fzf` TUI in `index.sh`.
- [ ] Ensure full compatibility with non-Termux Linux environments.
